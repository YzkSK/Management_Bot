{
  "name": "@management-bot/bot",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test",
    "dev": "bun run src/index.ts"
  },
  "dependencies": {
    "@management-bot/config": "workspace:*",
    "@management-bot/core": "workspace:*",
    "@management-bot/db": "workspace:*",
    "@management-bot/shared": "workspace:*",
    "discord.js": "^14.16.0"
  },
  "devDependencies": {
    "@trpc/server": "^11.0.0",
    "@types/bun": "^1.4.0",
    "@types/node": "^26.4.0",
    "typescript": "^6.0.3"
  }
}
