{
  "name": "@management-bot/dashboard-api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "bun test",
    "dev": "bun run src/index.ts"
  },
  "dependencies": {
    "@hono/trpc-server": "^0.4.0",
    "@management-bot/config": "workspace:*",
    "@management-bot/dashboard-access": "workspace:*",
    "@management-bot/db": "workspace:*",
    "@management-bot/shared": "workspace:*",
    "@trpc/server": "^11.0.0",
    "hono": "^4.6.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "@types/node": "^26.4.0",
    "typescript": "^6.0.3"
  }
}
